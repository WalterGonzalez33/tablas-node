# NOTAS:

```
Opciones:
      --help     Muestra ayuda                 [booleano]
      --version  Muestra número de versión     [booleano]
  -b, --base     el numero del cual quieres la tabla
                                     [número] [requerido]
  -l, --listar   mostrar la tabla en consola
                              [booleano] [defecto: false]
  -h, --hasta    hasta donde quieres multiplicar la base
                                   [número] [defecto: 10]
```